"""Arrivals computation.

Stub — implemented in issue #4. This module will combine parsed feed data
(feeds.py) with the configured stops/directions (stops.py + config.py) to produce
sorted per-line/direction arrival countdowns.
"""

# TODO(#4): compute per-station/direction arrival countdowns from feeds + config.
