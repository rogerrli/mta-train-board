# Example configuration for mta-train-board.
#
# Copy this file to `config.local.toml` and edit it for your setup.
# `config.local.toml` is gitignored so your personal settings stay local.
# If no `config.local.toml` exists, the app falls back to this example.

# How often the frontend should refresh the board, in seconds. The backend also
# polls the MTA feeds on this interval and caches the result (issue #6), so the
# API answers instantly instead of fetching live per request.
refresh_interval_seconds = 30

# How old cached data may get (seconds) before the API flags the board `stale` so
# the UI can show "data is old". Defaults to ~3x the refresh interval. During a
# feed outage the last-known board keeps being served, flagged stale past this.
stale_after_seconds = 90

# Best-case walk is this many minutes faster than a station's (worst-case)
# walk_minutes. It sets the "HURRY" band: a train arriving within this many
# minutes of your walk time is only makeable if you move fast. Default 1.
walk_best_case_delta_minutes = 1

# Transfer-crowding window, in minutes (issue #28; see [[transfer_crowding]]
# below). A feeder line's train arriving within this many minutes before one of
# your trains flags yours as "likely crowded"; your train arriving within this
# window before a feeder "beats the crowd". Default 2.
transfer_crowd_window_minutes = 2

# Focus mode (see [[focus]] below) plays an audible chime this many minutes
# before you need to leave to catch the recommended train, as a heads-up from
# across the room. The board beeps once per departure and re-arms when a later
# train becomes the pick. Default 10. Only fires for an on-time recommendation --
# never when the best train is already too late to make. Browsers block audio
# until a user gesture, so on first load the focused board shows a one-time
# "Enable sound" tap; a Chromium kiosk launched with
# --autoplay-policy=no-user-gesture-required skips that (see README).
alert_lead_minutes = 10

# Stations to watch. Add one [[stations]] block per station + line-set you care
# about. You identify stations by NAME and the LINES you want -- never by raw
# GTFS stop ID. At startup the app resolves each block to concrete stop IDs from
# the vendored MTA static station data (see app/stops.py).
#
#   name        Human station name, exactly as the MTA lists it (e.g.
#               "Grand Central-42 St", "Times Sq-42 St"). An unknown name
#               produces a helpful error suggesting close matches.
#   lines       The subway lines you want to see here (e.g. ["A", "C"]). Lines a
#               station serves but you omit are filtered out -- their trains
#               never show. A line the station does not serve is an error.
#   directions  Which platforms to show: "N" (northbound) and/or "S"
#               (southbound). Concrete stop IDs are the parent ID plus this
#               letter (e.g. "A41" + "N" -> "A41N").
#   walk_minutes  Optional. Worst-case minutes to walk here from home. The board
#               flags each train catchable / hurry / missed against it (see
#               walk_best_case_delta_minutes above). Omit it to leave arrivals
#               here unclassified.
#
# A single station can appear in multiple blocks when you want different lines in
# different directions (see Times Sq-42 St below); repeat the same
# walk_minutes on each block for that station.

# Example stations below use well-known Manhattan hubs so this file stays a
# generic demo. Copy it to config.local.toml and swap in your own stations.

# Times Sq-42 St: the 1/2/3 in both directions.
[[stations]]
name = "Times Sq-42 St"
lines = ["1", "2", "3"]
directions = ["N", "S"]
walk_minutes = 5

# Same station, the N/Q/R/W platform. List a station once per line-set you want;
# reuse the same walk time across its blocks.
[[stations]]
name = "Times Sq-42 St"
lines = ["N", "Q", "R", "W"]
directions = ["N", "S"]
walk_minutes = 5

# Grand Central-42 St: the 4/5/6, both directions.
[[stations]]
name = "Grand Central-42 St"
lines = ["4", "5", "6"]
directions = ["N", "S"]
walk_minutes = 8

# Fulton St: only the A/C here. Several other lines (2/3, 4/5, J/Z) also carry the
# name "Fulton St" on separate platforms; omitting them filters them out, and the
# lines you list disambiguate which "Fulton St" you mean.
[[stations]]
name = "Fulton St"
lines = ["A", "C"]
directions = ["N"]
walk_minutes = 4

# Transfer-crowding hints (issue #28) --------------------------------------
#
# At a transfer complex, a train on your line that pulls in right after another
# line's train tends to board crowded -- riders spill off the first train and
# pile onto yours. This flags those trains ("likely crowded -- follows the L")
# and marks a train that arrives ahead of the crowd, so the board can gently
# steer you to the comfortable one. It's purely advisory: it never hides or
# de-prioritizes a train you actually need to catch.
#
# Add one [[transfer_crowding]] block per (complex, line) you want annotated.
#
#   name     The transfer complex, by station NAME (same convention as
#            [[stations]] -- never a raw stop ID).
#   line     Your line at that complex -- the one whose trains get the hint.
#   feeders  The other lines there whose arriving riders crowd your line. A
#            feeder counts whichever direction it's going (a transfer crowd
#            spills both ways).
#
# Both `line` and every `feeder` must also be configured as [[stations]] at this
# complex (above), so their live arrivals are on the board -- the crowding check
# reads those, it doesn't fetch anything extra. The adjacency window is
# transfer_crowd_window_minutes (top of file).

# Example: at Times Sq-42 St the N/Q/R/W platform gets a wave off the 1/2/3, so
# flag the N when a 1/2/3 pulls in just before it. Both line-sets are configured
# as [[stations]] above. Swap in your own complex in config.local.toml.
[[transfer_crowding]]
name = "Times Sq-42 St"
line = "N"
feeders = ["1", "2", "3"]

# Direction labels. Riders think in terminals ("the uptown 1 to Van Cortlandt
# Park"), not compass words, so the board can label each line/direction by its
# TERMINAL station instead of "Northbound"/"Southbound". Add one
# [[direction_labels]] block per (line, direction) you want relabeled.
#
#   line       The subway line (e.g. "1").
#   direction  "N" or "S" -- the platform this label applies to.
#   terminal   The destination shown as the primary text. For a branchy
#              direction (e.g. the southbound A splits to Far Rockaway / Ozone
#              Park / Lefferts) write whatever combined label reads best, e.g.
#              terminal = "Rockaway / Lefferts".
#   borough    Smaller secondary text. Use the full borough name:
#              Manhattan / Brooklyn / Bronx / Queens / Staten Island.
#
# A (line, direction) with no block here falls back to "Northbound"/"Southbound",
# so you only need to label the ones you watch. The blocks below cover the
# generic stations configured above.

# 1/2/3 at Times Sq-42 St.
[[direction_labels]]
line = "1"
direction = "N"
terminal = "Van Cortlandt Park-242 St"
borough = "Bronx"

[[direction_labels]]
line = "1"
direction = "S"
terminal = "South Ferry"
borough = "Manhattan"

[[direction_labels]]
line = "2"
direction = "N"
terminal = "Wakefield-241 St"
borough = "Bronx"

[[direction_labels]]
line = "2"
direction = "S"
terminal = "Flatbush Av-Brooklyn College"
borough = "Brooklyn"

[[direction_labels]]
line = "3"
direction = "N"
terminal = "Harlem-148 St"
borough = "Manhattan"

[[direction_labels]]
line = "3"
direction = "S"
terminal = "New Lots Av"
borough = "Brooklyn"

# N/Q/R/W at Times Sq-42 St.
[[direction_labels]]
line = "N"
direction = "N"
terminal = "Astoria-Ditmars Blvd"
borough = "Queens"

[[direction_labels]]
line = "N"
direction = "S"
terminal = "Coney Island-Stillwell Av"
borough = "Brooklyn"

[[direction_labels]]
line = "Q"
direction = "N"
terminal = "96 St-2 Av"
borough = "Manhattan"

[[direction_labels]]
line = "Q"
direction = "S"
terminal = "Coney Island-Stillwell Av"
borough = "Brooklyn"

[[direction_labels]]
line = "R"
direction = "N"
terminal = "Forest Hills-71 Av"
borough = "Queens"

[[direction_labels]]
line = "R"
direction = "S"
terminal = "Bay Ridge-95 St"
borough = "Brooklyn"

[[direction_labels]]
line = "W"
direction = "N"
terminal = "Astoria-Ditmars Blvd"
borough = "Queens"

[[direction_labels]]
line = "W"
direction = "S"
terminal = "Whitehall St"
borough = "Manhattan"

# 4/5/6 at Grand Central-42 St.
[[direction_labels]]
line = "4"
direction = "N"
terminal = "Woodlawn"
borough = "Bronx"

[[direction_labels]]
line = "4"
direction = "S"
terminal = "Crown Hts-Utica Av"
borough = "Brooklyn"

[[direction_labels]]
line = "5"
direction = "N"
terminal = "Eastchester-Dyre Av"
borough = "Bronx"

[[direction_labels]]
line = "5"
direction = "S"
terminal = "Flatbush Av-Brooklyn College"
borough = "Brooklyn"

[[direction_labels]]
line = "6"
direction = "N"
terminal = "Pelham Bay Park"
borough = "Bronx"

[[direction_labels]]
line = "6"
direction = "S"
terminal = "Brooklyn Bridge-City Hall"
borough = "Manhattan"

# A/C at Fulton St (Manhattan-bound only above).
[[direction_labels]]
line = "A"
direction = "N"
terminal = "Inwood-207 St"
borough = "Manhattan"

[[direction_labels]]
line = "C"
direction = "N"
terminal = "168 St"
borough = "Manhattan"

# Arrive-by trips (issue #27) -----------------------------------------------
#
# A trip turns the board into a decision tool: "I need to be at X by 9:00 --
# which train do I catch?" Given a destination and a target arrival time, the app
# recommends the LATEST train that still gets you there on time (so you don't
# leave earlier than you must), plus a safer earlier fallback, and says clearly
# when even the best train arrives late. Add one [[trips]] block per commute.
#
#   name         Stable id for this trip (referenced by focus mode, issue #39).
#   boarding     Station you board at. Must match a [[stations]] block above by
#                NAME -- that's where the live countdowns and walk_minutes come
#                from. Configure that station with the trip's line + direction,
#                and give it a walk_minutes (arrive-by needs it).
#   line         The single line for the whole ride (no transfers -- one line per
#                trip for now).
#   direction    "N" or "S", matching the boarding station block.
#   destination  Where you're headed, by NAME (never a raw stop ID). Must be a
#                stop served by `line`. Rebuild travel times after adding a line
#                the vendored data wasn't trimmed to: `uv run scripts/refresh-travel-times`.
#   arrive_buffer_minutes  Optional. Aim to be there this many minutes EARLY
#                (the effective target is target - buffer). Default 0.
#   show_before_minutes  Optional (issue #50). Only show this trip's on-screen
#                recommendation strip in the last N minutes before the target,
#                then hide it -- so it doesn't sit on the board wasting space all
#                day. Omit to keep the strip up all day on a target day. (This is
#                a lighter touch than focus mode below, which takes the WHOLE
#                screen during its window; this just gates the strip.)
#   [trips.target]  Recurring target arrival time (Eastern, 24h "HH:MM"), keyed by
#                weekday abbreviation (mon/tue/wed/thu/fri/sat/sun) plus an
#                optional `default`. Resolution for today: an explicit weekday key
#                wins; otherwise a *weekday* (Mon-Fri) falls back to `default`; a
#                weekend with no explicit key has NO target (the board shows no
#                recommendation). So the block below means Tue 8:45, other
#                weekdays 9:00, and nothing on weekends.

# Example: board the A uptown at Fulton St (configured above) to 59 St-Columbus
# Circle -- aim to arrive by 8:45 on Tuesdays, 9:00 the rest of the week, and only
# surface the recommendation strip in the 90 minutes before that. These are
# placeholder stations/times; swap in your own trip in config.local.toml.
[[trips]]
name = "morning-uptown"
boarding = "Fulton St"
line = "A"
direction = "N"
destination = "59 St-Columbus Circle"
show_before_minutes = 90

[trips.target]
default = "09:00"
tue = "08:45"

# Scheduled focus mode (issue #39) --------------------------------------------
#
# By default the board is a general glance view. But for a known daily commute
# there's really only ONE train that matters in a given window. A focus rule
# dedicates the WHOLE screen to a single trip's arrive-by recommendation during a
# configured time window, hiding every other station and train -- then returns to
# the normal glance board when the window ends. Add one [[focus]] block per rule;
# with none, the board always shows the glance view (today's behavior).
#
#   trip   The [[trips]] name to focus on (above). The target arrival time is
#          owned by that trip -- a focus rule never redefines it, it just says
#          "when the board should dedicate itself to this trip".
#   days   Which weekdays the rule is active, as abbreviations (mon..sun).
#   start  Window start, Eastern 24h "HH:MM" (inclusive).
#   end    Window end, Eastern 24h "HH:MM" (exclusive). Must be after `start`;
#          a window crossing midnight isn't supported -- split it into two rules.
#
# The board re-checks the active rule on every refresh, so focus flips in/out
# right around the boundary. If two rules ever overlap, the FIRST one listed wins.

# Example: weekday mornings 08:00-09:00, focus the board on the morning-uptown A.
[[focus]]
trip = "morning-uptown"
days = ["mon", "tue", "wed", "thu", "fri"]
start = "08:00"
end = "09:00"
