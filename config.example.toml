# Example configuration for mta-train-board.
#
# Copy this file to `config.local.toml` and edit it for your setup.
# `config.local.toml` is gitignored so your personal settings stay local.
# If no `config.local.toml` exists, the app falls back to this example.

# How often the frontend should refresh the board, in seconds. The backend also
# polls the MTA feeds on this interval and caches the result (issue #6), so the
# API answers instantly instead of fetching live per request.
refresh_interval_seconds = 30

# How old cached data may get (seconds) before the API flags the board `stale` so
# the UI can show "data is old". Defaults to ~3x the refresh interval. During a
# feed outage the last-known board keeps being served, flagged stale past this.
stale_after_seconds = 90

# Best-case walk is this many minutes faster than a station's (worst-case)
# walk_minutes. It sets the "HURRY" band: a train arriving within this many
# minutes of your walk time is only makeable if you move fast. Default 1.
walk_best_case_delta_minutes = 1

# Stations to watch. Add one [[stations]] block per station + line-set you care
# about. You identify stations by NAME and the LINES you want -- never by raw
# GTFS stop ID. At startup the app resolves each block to concrete stop IDs from
# the vendored MTA static station data (see app/stops.py).
#
#   name        Human station name, exactly as the MTA lists it (e.g.
#               "Grand Central-42 St", "Times Sq-42 St"). An unknown name
#               produces a helpful error suggesting close matches.
#   lines       The subway lines you want to see here (e.g. ["A", "C"]). Lines a
#               station serves but you omit are filtered out -- their trains
#               never show. A line the station does not serve is an error.
#   directions  Which platforms to show: "N" (northbound) and/or "S"
#               (southbound). Concrete stop IDs are the parent ID plus this
#               letter (e.g. "A41" + "N" -> "A41N").
#   walk_minutes  Optional. Worst-case minutes to walk here from home. The board
#               flags each train catchable / hurry / missed against it (see
#               walk_best_case_delta_minutes above). Omit it to leave arrivals
#               here unclassified.
#
# A single station can appear in multiple blocks when you want different lines in
# different directions (see Times Sq-42 St below); repeat the same
# walk_minutes on each block for that station.

# Example stations below use well-known Manhattan hubs so this file stays a
# generic demo. Copy it to config.local.toml and swap in your own stations.

# Times Sq-42 St: the 1/2/3 in both directions.
[[stations]]
name = "Times Sq-42 St"
lines = ["1", "2", "3"]
directions = ["N", "S"]
walk_minutes = 5

# Same station, the N/Q/R/W platform. List a station once per line-set you want;
# reuse the same walk time across its blocks.
[[stations]]
name = "Times Sq-42 St"
lines = ["N", "Q", "R", "W"]
directions = ["N", "S"]
walk_minutes = 5

# Grand Central-42 St: the 4/5/6, both directions.
[[stations]]
name = "Grand Central-42 St"
lines = ["4", "5", "6"]
directions = ["N", "S"]
walk_minutes = 8

# Fulton St: only the A/C here. Several other lines (2/3, 4/5, J/Z) also carry the
# name "Fulton St" on separate platforms; omitting them filters them out, and the
# lines you list disambiguate which "Fulton St" you mean.
[[stations]]
name = "Fulton St"
lines = ["A", "C"]
directions = ["N"]
walk_minutes = 4

# Direction labels. Riders think in terminals ("the uptown 1 to Van Cortlandt
# Park"), not compass words, so the board can label each line/direction by its
# TERMINAL station instead of "Northbound"/"Southbound". Add one
# [[direction_labels]] block per (line, direction) you want relabeled.
#
#   line       The subway line (e.g. "1").
#   direction  "N" or "S" -- the platform this label applies to.
#   terminal   The destination shown as the primary text. For a branchy
#              direction (e.g. the southbound A splits to Far Rockaway / Ozone
#              Park / Lefferts) write whatever combined label reads best, e.g.
#              terminal = "Rockaway / Lefferts".
#   borough    Smaller secondary text. Convention: Man / Bklyn / Bx / Qns / SI.
#
# A (line, direction) with no block here falls back to "Northbound"/"Southbound",
# so you only need to label the ones you watch. The blocks below cover the
# generic stations configured above.

# 1/2/3 at Times Sq-42 St.
[[direction_labels]]
line = "1"
direction = "N"
terminal = "Van Cortlandt Park-242 St"
borough = "Bx"

[[direction_labels]]
line = "1"
direction = "S"
terminal = "South Ferry"
borough = "Man"

[[direction_labels]]
line = "2"
direction = "N"
terminal = "Wakefield-241 St"
borough = "Bx"

[[direction_labels]]
line = "2"
direction = "S"
terminal = "Flatbush Av-Brooklyn College"
borough = "Bklyn"

[[direction_labels]]
line = "3"
direction = "N"
terminal = "Harlem-148 St"
borough = "Man"

[[direction_labels]]
line = "3"
direction = "S"
terminal = "New Lots Av"
borough = "Bklyn"

# N/Q/R/W at Times Sq-42 St.
[[direction_labels]]
line = "N"
direction = "N"
terminal = "Astoria-Ditmars Blvd"
borough = "Qns"

[[direction_labels]]
line = "N"
direction = "S"
terminal = "Coney Island-Stillwell Av"
borough = "Bklyn"

[[direction_labels]]
line = "Q"
direction = "N"
terminal = "96 St-2 Av"
borough = "Man"

[[direction_labels]]
line = "Q"
direction = "S"
terminal = "Coney Island-Stillwell Av"
borough = "Bklyn"

[[direction_labels]]
line = "R"
direction = "N"
terminal = "Forest Hills-71 Av"
borough = "Qns"

[[direction_labels]]
line = "R"
direction = "S"
terminal = "Bay Ridge-95 St"
borough = "Bklyn"

[[direction_labels]]
line = "W"
direction = "N"
terminal = "Astoria-Ditmars Blvd"
borough = "Qns"

[[direction_labels]]
line = "W"
direction = "S"
terminal = "Whitehall St"
borough = "Man"

# 4/5/6 at Grand Central-42 St.
[[direction_labels]]
line = "4"
direction = "N"
terminal = "Woodlawn"
borough = "Bx"

[[direction_labels]]
line = "4"
direction = "S"
terminal = "Crown Hts-Utica Av"
borough = "Bklyn"

[[direction_labels]]
line = "5"
direction = "N"
terminal = "Eastchester-Dyre Av"
borough = "Bx"

[[direction_labels]]
line = "5"
direction = "S"
terminal = "Flatbush Av-Brooklyn College"
borough = "Bklyn"

[[direction_labels]]
line = "6"
direction = "N"
terminal = "Pelham Bay Park"
borough = "Bx"

[[direction_labels]]
line = "6"
direction = "S"
terminal = "Brooklyn Bridge-City Hall"
borough = "Man"

# A/C at Fulton St (Manhattan-bound only above).
[[direction_labels]]
line = "A"
direction = "N"
terminal = "Inwood-207 St"
borough = "Man"

[[direction_labels]]
line = "C"
direction = "N"
terminal = "168 St"
borough = "Man"
